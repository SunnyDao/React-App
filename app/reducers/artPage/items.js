import * as ActionTypes from '../../actions/artPage';


export default function (state = initialState, action) {
    switch (action.type) {
      case ART_CREATE_ITEM:
          return state;
      default:
          return state;
    }
}
